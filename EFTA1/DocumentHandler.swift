//
//  DocumentHandler.swift
//  EFTA1
//
//  Created by Joshua on 4/11/24.
//

import SwiftUI

import Foundation
/*
protocol DocumentHandling: AnyObject {
    var selectedDocuments: [URL] { get set }
    func addDocuments(_ documents: [URL])
    func clearDocuments()
}


class DocumentHandler: ObservableObject, DocumentHandling {
    @Published var selectedDocuments: [URL] = []

    func addDocuments(_ documents: [URL]) {
        self.selectedDocuments.append(contentsOf: documents)
    }

    func clearDocuments() {
        self.selectedDocuments.removeAll()
    }
}

class CustomerMonitoringDocumentHandler: ObservableObject, DocumentHandling {
    @Published var selectedDocuments: [URL] = []

    func addDocuments(_ documents: [URL]) {
        print("testing to see if it working ")
        self.selectedDocuments.append(contentsOf: documents)
        print(self.selectedDocuments)
    }

    func clearDocuments() {
        self.selectedDocuments.removeAll()
    }
}
 */
